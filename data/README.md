# DraftOrbit Local Data

Local datasets and runtime data belong here.

This directory is not the canonical source for project documentation and should stay out of git unless explicitly approved. `.gitignore` keeps this README trackable while ignoring local data payloads by default.
